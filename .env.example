# Vercel: set these in Project → Settings → Environment Variables (not only in server/.env).

MIRELO_API_KEY=
MIRELO_API_BASE_URL=https://api.mirelo.ai

# Local server only (ignored on Vercel)
PORT=8788
